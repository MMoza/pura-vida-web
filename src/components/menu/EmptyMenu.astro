<div class="relative flex flex-col items-center justify-center text-center py-20 px-6 overflow-hidden">

  <!-- Fondo animado -->
  <div class="absolute inset-0 -z-10">
    <div class="blob blob-1"></div>
    <div class="blob blob-2"></div>
  </div>

  <!-- Icono -->
  <div class="icon-float w-16 h-16 mb-6 rounded-full bg-white/60 backdrop-blur flex items-center justify-center shadow-sm">
    <span class="text-3xl">🍽️</span>
  </div>

  <!-- Título -->
  <h3 class="text-2xl md:text-3xl font-display font-semibold text-gray-900">
    Carta no disponible
  </h3>

  <!-- Texto -->
  <p class="text-gray-600 mt-3 max-w-md text-sm md:text-base leading-relaxed">
    No tenemos platos disponibles en este momento.  
    Estamos preparando algo especial para ti.
  </p>

  <!-- Línea decorativa -->
  <div class="mt-6 w-16 h-[2px] bg-gray-300 rounded-full"></div>

</div>

<style>
/* 🔵 Blobs suaves */
.blob {
  position: absolute;
  width: 200px;
  height: 200px;
  background: #ffffff;
  opacity: 0.15;
  filter: blur(40px);
  border-radius: 50%;
  animation: blobMove 12s ease-in-out infinite;
}

.blob-1 {
  top: 10%;
  left: 20%;
}

.blob-2 {
  bottom: 10%;
  right: 20%;
  animation-delay: 4s;
}

/* Movimiento orgánico */
@keyframes blobMove {
  0%, 100% {
    transform: translate(0, 0) scale(1);
  }
  25% {
    transform: translate(20px, -30px) scale(1.1);
  }
  50% {
    transform: translate(-10px, 20px) scale(0.95);
  }
  75% {
    transform: translate(15px, 10px) scale(1.05);
  }
}

/* 🍽️ Icono flotante */
.icon-float {
  animation: float 3s ease-in-out infinite;
}

@keyframes float {
  0%, 100% {
    transform: translateY(0px);
  }
  50% {
    transform: translateY(-6px);
  }
}
</style>